---
export function getStaticPaths() {
  return [
    { params: { lang: undefined } },
    { params: { lang: 'id' } },
    { params: { lang: 'es' } },
  ];
}

const { lang: langParam } = Astro.params;
const lang = langParam || 'en';

import ToolLayout from '../../../layouts/ToolLayout.astro';
import Vectorizer from '../../../components/tools/Vectorizer';
import { useTranslations } from '../../../lib/i18n';
const t = useTranslations(lang as any);
---

<ToolLayout 
	title={t('tool.vectorizer.title')}
	description={t('tool.vectorizer.desc')}
	category="image"
	toolId="vectorizer"
>
	<Vectorizer client:only="react" lang={lang as any} />

	<div slot="guide">
		<h3>{t('guide.vectorizer.h1')}</h3>
		<p>{t('guide.vectorizer.p1')}</p>
		
		<ul>
			<li><strong>{t('guide.vectorizer.f1_t')}</strong> {t('guide.vectorizer.f1_d')}</li>
			<li><strong>{t('guide.vectorizer.f2_t')}</strong> {t('guide.vectorizer.f2_d')}</li>
			<li><strong>{t('guide.vectorizer.f3_t')}</strong> {t('guide.vectorizer.f3_d')}</li>
		</ul>
	</div>
</ToolLayout>
